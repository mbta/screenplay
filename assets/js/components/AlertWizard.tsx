import React from 'react';
import SimpleForm from './SimpleForm';

interface AlertWizardProps {}

const AlertWizard = (props: AlertWizardProps): JSX.Element => {
  return (
    <SimpleForm />
  )
};

export default AlertWizard;